import { SequenceDatePipe } from './sequence-date.pipe';

describe('SequenceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SequenceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
