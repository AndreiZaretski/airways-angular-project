import { NextDayArrivalPipe } from './next-day-arrival.pipe';

describe('NextDayArrivalPipe', () => {
  it('create an instance', () => {
    const pipe = new NextDayArrivalPipe();
    expect(pipe).toBeTruthy();
  });
});
