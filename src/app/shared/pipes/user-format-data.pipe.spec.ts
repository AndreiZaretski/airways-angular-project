import { UserFormatDataPipe } from './user-format-data.pipe';

describe('UserFormatDataPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFormatDataPipe();
    expect(pipe).toBeTruthy();
  });
});
