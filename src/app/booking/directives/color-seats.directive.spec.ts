import { ColorSeatsDirective } from './color-seats.directive';

describe('ColorSeatsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorSeatsDirective();
    expect(directive).toBeTruthy();
  });
});
