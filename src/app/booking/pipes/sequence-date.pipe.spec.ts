import { SecuenceDatePipe } from './sequence-date.pipe';

describe('SecuenceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SecuenceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
