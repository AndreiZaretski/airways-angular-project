import { TotalPassengersPipe } from './total-passengers.pipe';

describe('TotalPassengersPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPassengersPipe();
    expect(pipe).toBeTruthy();
  });
});
